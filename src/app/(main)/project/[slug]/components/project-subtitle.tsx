export default function ProjectSubtitle({ children }: { children: any }) {
  return (
    <div className="w-full bg-soft px-4 md:px-10 py-1 mt-8 mb-1 text-title-l font-extrabold text-start">
      {children}
    </div>
  )
}
